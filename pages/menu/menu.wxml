<view class="container">
  <view class="back-bar" bindtap="goBack">
    <text class="back-icon">⬅</text> 返回首页
  </view>
  <view class="header">🏠 今日家常菜</view>
  
  <view class="menu-list">
    <block wx:for="{{menuItems}}" wx:key="id">
      <view class="menu-item">
        <text class="dish-name">{{item.name}}</text>
        
        <view class="stepper">
          <view 
            class="minus {{item.count > 0 ? '' : 'disabled'}}" 
            bindtouchstart="startTimer" 
            bindtouchend="stopTimer" 
            bindtouchcancel="stopTimer"
            data-id="{{item.id}}" 
            data-type="minus">-</view>

          <text class="count" wx:if="{{item.count > 0}}">{{item.count}}</text>

          <view 
            class="plus" 
            bindtouchstart="startTimer" 
            bindtouchend="stopTimer" 
            bindtouchcancel="stopTimer"
            data-id="{{item.id}}" 
            data-type="plus">+</view>
        </view>
      </view>
    </block>
  </view>

  <view class="footer">
    <text>总共：{{totalCount}} 份菜</text>
    <button class="submit-btn" bindtap="submitOrder">确定点餐</button>
  </view>
</view>